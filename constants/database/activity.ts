// Exports:
/**
 * The minimum number of activities that can be present for a user, at which the feed is stable.
 */
export const STABLE_RECENT_USER_ACTIVITY_COUNT = 100

/**
 * The maximum number of activities that can be present for a user, before which it is pruned to `STABLE_RECENT_USER_ACTIVITY_COUNT`.
 */
export const MAX_RECENT_USER_ACTIVITY_COUNT = 250
